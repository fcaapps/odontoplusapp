import 'package:flutter/material.dart';

import '../../../main.dart';

class userpassLogin extends StatefulWidget {
  final TextEditingController usurController;

  const userpassLogin({Key key, this.usurController}) : super(key: key);
  @override
  _userpassLoginState createState() => _userpassLoginState();
}

class _userpassLoginState extends State<userpassLogin> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
